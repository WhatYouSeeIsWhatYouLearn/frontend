/* eslint-disable import/no-anonymous-default-export */
const initialState = {
    templates: []
}

export default (state = initialState, action: any) => {
    switch(action.type){
        
        default:
            return state;
    }
}